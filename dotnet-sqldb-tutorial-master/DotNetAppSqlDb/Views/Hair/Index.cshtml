@model IEnumerable<DotNetAppSqlDb.Models.Hair>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HairColour)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HairLength)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HairColour)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HairLength)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HairID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HairID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HairID })
        </td>
    </tr>
}

</table>
